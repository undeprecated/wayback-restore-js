var assert = require("chai").assert;
var expect = require("chai").expect;

var Wayback = require("../");
var asset = require("../lib/asset");

module.exports = {
    assert: assert,
    expect: expect,
    asset: asset
};